# ExamPro web — only needed for a split deployment (Vercel frontend ↔ Render API).
# Leave empty/unset when the API serves this SPA directly (local dev, Track A).
#
# Option 1 (recommended): no value here — proxy /api via web/vercel.json instead.
# Option 2: direct cross-origin calls to the Render API:
# VITE_API_BASE=https://exampro-api.onrender.com
